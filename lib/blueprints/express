#!/usr/bin/env bash

# Function to setup express.js project
setup_express(){
    project_name="$1"

    echo "Initializing express.js project..."

    mkdir -p "$project_name"/src/routes \
    	     "$project_name"/src/controllers \
	     "$project_name"/src/middleware \
	     "$project_name"/src/types \
	     "$project_name"/src/models \
	     "$project_name"/public/css \
	     "$project_name"/views

    # Check that the directories exist
    if [[ -d "$project_name"/src/routes \
	    && -d "$project_name"/src/controllers \
	    && -d "$project_name"/src/middleware \
	    && -d "$project_name"/src/types \
	    && -d "$project_name"/src/models \
	    && -d "$project_name"/public/css \
	    && -d "$project_name"/views ]]; then

        echo "Project directories successfully created."
	echo "Creating project files..."

    	touch "$project_name"/.env \
            "$project_name"/.gitignore \
	    "$project_name"/README.md \
	    "$project_name"/app.ts \
	    "$project_name"/package.json \
	    "$project_name"/tsconfig.json \
	    "$project_name"/views/index.ejs \
	    "$project_name"/public/css/styles.css

	# Check that the files exist
	if [[ -f "$project_name"/.env \
		&& -f "$project_name"/.gitignore \
		&& -f "$project_name"/README.md \
		&& -f "$project_name"/app.ts \
		&& -f "$project_name"/package.json \
		&& -f "$project_name"/tsconfig.json \
		&& -f "$project_name"/views/index.ejs \
		&& -f "$project_name"/public/css/styles.css ]]; then


    	    cat << EOF > "$project_name"/app.ts
import express, { Request, Response } from 'express';
import dotenv from 'dotenv';

dotenv.config();

const app = express();
const PORT = process.env.PORT || 3000;

app.get('/', (req: Request, res: Response) => {
    res.send('"$project_name" TypeScript Express Server is Live!');
});

app.listen(PORT, () => {
    console.log(\`[server]: Server is running at http://localhost:\${PORT}\`);
});
EOF

	    cat << EOF > "$project_name"/package.json
{
  "name": "$project_name",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/app.js",
    "dev": "ts-node-dev --respawn app.ts"
  },
  "dependencies": {
    "express": "^4.18.2",
    "dotenv": "^16.0.3"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^20.2.5",
    "typescript": "^5.0.4",
    "ts-node-dev": "^2.0.0"
  }
}
EOF

	    cat << EOF > "$project_name"/tsconfig.json
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "commonjs",
    "outDir": "./dist",
    "rootDir": "./src",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true
  },
  "include": ["src/**/*"]
}
EOF

	    cat << EOF > "$project_name"/views/index.ejs
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Index</title>
    <link rel="stylesheet" href="/public/styles.css">
</head>
<body>
    
</body>
</html>
EOF

            echo "# "$project_name"" > "$project_name"/README.md
            echo "Express.js project is ready!"
        else
	    echo "Error: Files were not created. Please check your storage." >&2
	    exit 1
	fi
    else
        echo "Error: Directories were not created. Please check your storage." >&2
	exit 1
    fi
}
